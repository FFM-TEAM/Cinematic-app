import * as React from 'react';
import styled from 'styled-components';

const MovieTrailerTemplateBlock = styled.div``;
export interface MovieTrailerTemplateProps {}

const MovieTrailerTemplate: React.FC<MovieTrailerTemplateProps> = props => {
  return <MovieTrailerTemplateBlock />;
};

export default MovieTrailerTemplate;
